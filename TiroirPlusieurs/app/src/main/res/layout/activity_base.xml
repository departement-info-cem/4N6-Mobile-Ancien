<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawerLayoutID"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    tools:openDrawer="start">
    <!-- TODO peut pas faire include ici, parce qu'on ne peut pas mettre plus qu'un fichier layout à la fois -->

    <!-- TODO La stratégie est d'utiliser un frameLayout vide qui sera inflate par le layout de l'activity qu'on veut -->
    <FrameLayout
        android:id="@+id/frameLayoutID"
        android:layout_width="wrap_content"
        android:layout_height="match_parent" />


    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationViewID"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        app:menu="@menu/menu">
    </com.google.android.material.navigation.NavigationView>

</androidx.drawerlayout.widget.DrawerLayout>